name = 'resource_test'

literal('literal') = run1()
literal('other_literal') = run2()

argument(@pos_arg, vec3i) {
  map_type = result.apply()

  executes = runPos()

  argument('other_arg', entities) {
    map_type = result.findEntities()
    executes = runEntities()
  }
}